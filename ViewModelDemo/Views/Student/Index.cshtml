@using ViewModelDemo.Models
@model StudentViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Name
            </th>
            <th>
               City
            </th>
             <th>
               Messages AllStudents
            </th>
             <th>
               Messages PartialStudents
            </th>
            <th>
               Messages ForeignStudents
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Student) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
               
                   @foreach (var item1 in Model.Messages)
                {
                    <td>
                       @Html.DisplayFor(modelItem => item1.Allstudents)
                    </td>
                     <td>
                       @Html.DisplayFor(modelItem => item1.PartialStudents)
                    </td>
                     <td>
                       @Html.DisplayFor(modelItem => item1.ForeignStudents)
                    </td>
                }
               
                <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
 

    </tbody>
</table>
